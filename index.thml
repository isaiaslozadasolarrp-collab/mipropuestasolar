<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>IsaiasLozadaSolarPR — Empieza a ahorrar hoy</title>
<style>
  @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap');

  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

  :root {
    --navy: #0A1628;
    --navy-mid: #112240;
    --solar: #FFB800;
    --solar-light: #FFD166;
    --white: #FFFFFF;
    --green: #22C55E;
    --gray-bg: #F0F4FF;
    --gray-text: #64748B;
    --border: #E2E8F0;
    --danger: #EF4444;
  }

  body {
    font-family: 'Inter', sans-serif;
    background: linear-gradient(160deg, #0A1628 0%, #0D1F3C 40%, #111832 70%, #0A1628 100%);
    color: var(--white);
    min-height: 100vh;
    overflow-x: hidden;
  }

  /* ── HERO ── */
  .hero {
    position: relative;
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding: 2rem 1.5rem 6rem;
    overflow: hidden;
  }

  .hero::before {
    content: '';
    position: absolute;
    inset: 0;
    background: radial-gradient(ellipse 80% 60% at 50% 0%, rgba(255,184,0,0.18) 0%, transparent 70%),
                radial-gradient(ellipse 60% 40% at 80% 100%, rgba(34,197,94,0.08) 0%, transparent 60%);
    pointer-events: none;
  }

  .sun-orb {
    position: absolute;
    top: -60px;
    left: 50%;
    transform: translateX(-50%);
    width: 320px;
    height: 320px;
    border-radius: 50%;
    background: radial-gradient(circle, #FFD166 0%, #FFB800 45%, rgba(255,184,0,0.1) 100%);
    filter: blur(2px);
    animation: pulse-sun 4s ease-in-out infinite;
    z-index: 0;
    pointer-events: none;
  }

  @keyframes pulse-sun {
    0%, 100% { box-shadow: 0 0 60px 20px rgba(255,184,0,0.3), 0 0 120px 60px rgba(255,184,0,0.15); }
    50% { box-shadow: 0 0 80px 30px rgba(255,184,0,0.45), 0 0 160px 80px rgba(255,184,0,0.2); }
  }

  .logo {
    position: relative;
    z-index: 1;
    font-size: 0.85rem;
    font-weight: 700;
    letter-spacing: 0.25em;
    color: var(--solar);
    text-transform: uppercase;
    margin-bottom: 3.5rem;
    opacity: 0;
    animation: fadeUp 0.6s 0.2s forwards;
  }

  .hero-tag {
    position: relative;
    z-index: 1;
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    background: rgba(255,184,0,0.12);
    border: 1px solid rgba(255,184,0,0.3);
    border-radius: 100px;
    padding: 0.35rem 1rem;
    font-size: 0.78rem;
    font-weight: 600;
    color: var(--solar-light);
    letter-spacing: 0.05em;
    margin-bottom: 1.5rem;
    opacity: 0;
    animation: fadeUp 0.6s 0.4s forwards;
  }

  .dot-live {
    width: 7px; height: 7px;
    border-radius: 50%;
    background: var(--green);
    animation: blink 1.5s infinite;
  }
  @keyframes blink { 0%,100% { opacity:1 } 50% { opacity:0.3 } }

  h1 {
    position: relative;
    z-index: 1;
    font-size: clamp(2.4rem, 7vw, 4.5rem);
    font-weight: 900;
    line-height: 1.05;
    letter-spacing: -0.03em;
    max-width: 760px;
    opacity: 0;
    animation: fadeUp 0.7s 0.6s forwards;
  }

  h1 span { color: #3DBD4A; }

  .hero-sub {
    position: relative;
    z-index: 1;
    margin-top: 1.25rem;
    font-size: clamp(1rem, 2.5vw, 1.2rem);
    color: rgba(255,255,255,0.65);
    max-width: 520px;
    line-height: 1.6;
    opacity: 0;
    animation: fadeUp 0.7s 0.8s forwards;
  }

  .cta-btn {
    position: relative;
    z-index: 1;
    margin-top: 2.5rem;
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    background: var(--solar);
    color: var(--navy);
    font-size: 1.05rem;
    font-weight: 800;
    padding: 1rem 2.2rem;
    border-radius: 100px;
    border: none;
    cursor: pointer;
    transition: transform 0.2s, box-shadow 0.2s;
    text-decoration: none;
    box-shadow: 0 0 0 0 rgba(255,184,0,0.4);
    animation: fadeUp 0.7s 1s forwards, glow-cta 2.5s 1.7s ease-in-out infinite;
    opacity: 0;
  }
  @keyframes glow-cta {
    0%,100% { box-shadow: 0 4px 24px rgba(255,184,0,0.35); }
    50% { box-shadow: 0 4px 40px rgba(255,184,0,0.6); }
  }
  .cta-btn:hover { transform: translateY(-2px) scale(1.03); }

  .arrow-icon { font-size: 1.1rem; transition: transform 0.2s; }
  .cta-btn:hover .arrow-icon { transform: translateX(4px); }

  .trust-strip {
    position: relative;
    z-index: 1;
    margin-top: 2rem;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1.5rem;
    flex-wrap: wrap;
    opacity: 0;
    animation: fadeUp 0.7s 1.2s forwards;
  }

  .trust-item {
    display: flex;
    align-items: center;
    gap: 0.4rem;
    font-size: 0.8rem;
    color: rgba(255,255,255,0.5);
  }
  .trust-item span { color: var(--green); font-size: 0.9rem; }


  /* ── BRAND LOGO ── */
  .brand-logo-wrap {
    position: relative;
    z-index: 1;
    margin-top: 2.5rem;
    margin-bottom: -70px;
    display: flex;
    justify-content: center;
    opacity: 0;
    animation: fadeUp 0.7s 1.4s forwards;
  }

  .brand-logo {
    width: 110px;
    height: 110px;
    object-fit: contain;
    mix-blend-mode: screen;
    opacity: 0.88;
    filter: brightness(1.1) saturate(1.15);
  }

  /* ── SCROLL ARROW ── */
  .scroll-hint {
    position: absolute;
    bottom: 2rem;
    left: 50%;
    transform: translateX(-50%);
    z-index: 1;
    opacity: 0.4;
    animation: bounce 2s infinite;
    font-size: 1.5rem;
  }
  @keyframes bounce { 0%,100% { transform: translateX(-50%) translateY(0) } 50% { transform: translateX(-50%) translateY(8px) } }

  /* ── FORM SECTION ── */
  .form-section {
    background: var(--gray-bg);
    padding: 5rem 1.5rem;
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .section-label {
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.2em;
    color: var(--solar);
    text-transform: uppercase;
    margin-bottom: 0.75rem;
  }

  .section-title {
    font-size: clamp(1.6rem, 4vw, 2.4rem);
    font-weight: 800;
    color: var(--navy);
    text-align: center;
    letter-spacing: -0.02em;
    max-width: 560px;
    line-height: 1.2;
    margin-bottom: 0.75rem;
  }

  .section-sub {
    color: var(--gray-text);
    font-size: 0.95rem;
    text-align: center;
    max-width: 440px;
    margin-bottom: 3rem;
    line-height: 1.6;
  }

  /* ── STEPS PROGRESS ── */
  .steps-bar {
    display: flex;
    align-items: center;
    gap: 0;
    margin-bottom: 2.5rem;
    width: 100%;
    max-width: 540px;
  }

  .step-node {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.4rem;
    flex: 1;
    position: relative;
    cursor: default;
  }

  .step-circle {
    width: 44px; height: 44px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 800;
    font-size: 0.9rem;
    transition: all 0.4s;
    border: 2px solid var(--border);
    background: white;
    color: #CBD5E1;
    position: relative;
    z-index: 1;
  }

  .step-circle.active {
    border-color: var(--solar);
    background: var(--solar);
    color: var(--navy);
    box-shadow: 0 0 0 6px rgba(255,184,0,0.18);
  }

  .step-circle.done {
    border-color: var(--green);
    background: var(--green);
    color: white;
  }

  .step-label {
    font-size: 0.7rem;
    font-weight: 600;
    color: #94A3B8;
    text-align: center;
    transition: color 0.3s;
    white-space: nowrap;
  }
  .step-node.is-active .step-label { color: var(--navy); }
  .step-node.is-done .step-label { color: var(--green); }

  .step-line {
    flex: 1;
    height: 2px;
    background: var(--border);
    margin-bottom: 1.6rem;
    transition: background 0.4s;
    position: relative;
    overflow: hidden;
  }
  .step-line .fill {
    position: absolute;
    top: 0; left: 0;
    height: 100%;
    width: 0%;
    background: var(--green);
    transition: width 0.5s ease;
  }

  /* ── CARD ── */
  .card {
    background: white;
    border-radius: 24px;
    padding: 2.5rem 2rem;
    width: 100%;
    max-width: 540px;
    box-shadow: 0 8px 40px rgba(10,22,40,0.10);
    position: relative;
    overflow: hidden;
  }

  .card::before {
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0;
    height: 4px;
    background: linear-gradient(90deg, var(--solar), var(--green));
  }

  .step-panel { display: none; }
  .step-panel.active { display: block; animation: slideIn 0.4s ease; }

  @keyframes slideIn {
    from { opacity: 0; transform: translateX(20px); }
    to   { opacity: 1; transform: translateX(0); }
  }

  .panel-icon {
    font-size: 2.5rem;
    margin-bottom: 0.75rem;
  }

  .panel-title {
    font-size: 1.35rem;
    font-weight: 800;
    color: var(--navy);
    margin-bottom: 0.35rem;
    letter-spacing: -0.02em;
  }

  .panel-desc {
    font-size: 0.88rem;
    color: var(--gray-text);
    margin-bottom: 1.75rem;
    line-height: 1.55;
  }

  label {
    display: block;
    font-size: 0.82rem;
    font-weight: 700;
    color: var(--navy);
    margin-bottom: 0.4rem;
    letter-spacing: 0.02em;
  }

  input[type="tel"],
  input[type="number"],
  input[type="text"] {
    width: 100%;
    padding: 0.85rem 1rem;
    border: 2px solid var(--border);
    border-radius: 12px;
    font-size: 1rem;
    font-family: 'Inter', sans-serif;
    color: var(--navy);
    outline: none;
    transition: border-color 0.2s, box-shadow 0.2s;
    background: #FAFBFF;
    margin-bottom: 1.25rem;
  }

  input:focus {
    border-color: var(--solar);
    box-shadow: 0 0 0 4px rgba(255,184,0,0.15);
  }

  .hint {
    font-size: 0.75rem;
    color: #94A3B8;
    margin-top: -1rem;
    margin-bottom: 1.25rem;
  }

  /* Upload zone */
  .upload-zone {
    border: 2px dashed var(--border);
    border-radius: 16px;
    padding: 2rem 1.5rem;
    text-align: center;
    cursor: pointer;
    transition: border-color 0.25s, background 0.25s;
    background: #FAFBFF;
    position: relative;
    margin-bottom: 1rem;
  }
  .upload-zone:hover, .upload-zone.drag-over {
    border-color: var(--solar);
    background: rgba(255,184,0,0.04);
  }

  .upload-zone input[type="file"] {
    position: absolute;
    inset: 0;
    opacity: 0;
    cursor: pointer;
    width: 100%;
    height: 100%;
    margin: 0;
    padding: 0;
    border: none;
  }

  .upload-icon { font-size: 2rem; margin-bottom: 0.5rem; }
  .upload-text { font-size: 0.9rem; font-weight: 600; color: var(--navy); }
  .upload-sub { font-size: 0.75rem; color: #94A3B8; margin-top: 0.25rem; }

  .file-preview {
    display: none;
    align-items: center;
    gap: 0.75rem;
    background: rgba(34,197,94,0.08);
    border: 1.5px solid rgba(34,197,94,0.3);
    border-radius: 10px;
    padding: 0.75rem 1rem;
    margin-bottom: 1rem;
    font-size: 0.85rem;
    color: var(--navy);
    font-weight: 600;
  }
  .file-preview.visible { display: flex; }
  .file-preview .remove-file {
    margin-left: auto;
    cursor: pointer;
    color: #94A3B8;
    font-size: 1rem;
    background: none;
    border: none;
    padding: 0;
    line-height: 1;
  }
  .file-preview .remove-file:hover { color: var(--danger); }

  /* Ping field */
  .ping-section { margin-bottom: 1.25rem; }
  .ping-info {
    background: rgba(255,184,0,0.07);
    border-left: 3px solid var(--solar);
    border-radius: 0 8px 8px 0;
    padding: 0.65rem 0.9rem;
    font-size: 0.78rem;
    color: #78716C;
    margin-top: -0.9rem;
    margin-bottom: 1.25rem;
    line-height: 1.5;
  }

  /* Button */
  .btn-next {
    width: 100%;
    padding: 1rem;
    border-radius: 14px;
    font-size: 1rem;
    font-weight: 800;
    border: none;
    cursor: pointer;
    transition: transform 0.15s, box-shadow 0.15s;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    background: var(--solar);
    color: var(--navy);
    box-shadow: 0 4px 20px rgba(255,184,0,0.3);
    letter-spacing: 0.01em;
  }
  .btn-next:hover { transform: translateY(-1px); box-shadow: 0 6px 28px rgba(255,184,0,0.45); }
  .btn-next:disabled { opacity: 0.5; cursor: not-allowed; transform: none; }

  .btn-back {
    width: 100%;
    padding: 0.8rem;
    border-radius: 14px;
    font-size: 0.9rem;
    font-weight: 600;
    border: 1.5px solid var(--border);
    background: white;
    color: var(--gray-text);
    cursor: pointer;
    margin-top: 0.75rem;
    transition: border-color 0.2s;
  }
  .btn-back:hover { border-color: #CBD5E1; }

  /* Error */
  .error-msg {
    color: var(--danger);
    font-size: 0.78rem;
    font-weight: 600;
    margin-top: -1rem;
    margin-bottom: 1rem;
    display: none;
  }
  .error-msg.show { display: block; }

  /* ── SUCCESS ── */
  .success-panel { display: none; }
  .success-panel.active { display: flex; flex-direction: column; align-items: center; text-align: center; padding: 1rem 0; animation: slideIn 0.5s ease; }

  .success-ring {
    width: 80px; height: 80px;
    border-radius: 50%;
    background: rgba(34,197,94,0.12);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 2.5rem;
    margin-bottom: 1.25rem;
    animation: pop 0.5s cubic-bezier(.175,.885,.32,1.275);
  }
  @keyframes pop { from { transform: scale(0.4); opacity:0 } to { transform: scale(1); opacity:1 } }

  .success-title {
    font-size: 1.5rem;
    font-weight: 800;
    color: var(--navy);
    margin-bottom: 0.5rem;
  }

  .success-sub {
    font-size: 0.9rem;
    color: var(--gray-text);
    line-height: 1.6;
    max-width: 360px;
    margin-bottom: 1.5rem;
  }

  .next-steps {
    width: 100%;
    background: #F8FAFC;
    border-radius: 14px;
    padding: 1.25rem 1.5rem;
    text-align: left;
  }

  .next-steps h4 {
    font-size: 0.75rem;
    font-weight: 700;
    color: var(--gray-text);
    text-transform: uppercase;
    letter-spacing: 0.15em;
    margin-bottom: 0.9rem;
  }

  .next-step-item {
    display: flex;
    align-items: flex-start;
    gap: 0.75rem;
    margin-bottom: 0.75rem;
    font-size: 0.85rem;
    color: var(--navy);
  }
  .next-step-item:last-child { margin-bottom: 0; }
  .ns-icon { font-size: 1rem; flex-shrink: 0; margin-top: 0.05rem; }
  .ns-text strong { display: block; font-weight: 700; margin-bottom: 0.1rem; }
  .ns-text span { color: var(--gray-text); font-size: 0.78rem; }

  /* ── STATS ── */
  .stats-section {
    background: var(--navy-mid);
    padding: 4rem 1.5rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 2.5rem;
  }

  .stats-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
    gap: 1.5rem;
    width: 100%;
    max-width: 860px;
  }

  .stat-card {
    background: rgba(255,255,255,0.04);
    border: 1px solid rgba(255,255,255,0.08);
    border-radius: 20px;
    padding: 1.75rem 1.5rem;
    text-align: center;
  }

  .stat-num {
    font-size: 2.2rem;
    font-weight: 900;
    color: var(--solar);
    letter-spacing: -0.04em;
    line-height: 1;
    margin-bottom: 0.4rem;
  }

  .stat-label {
    font-size: 0.82rem;
    color: rgba(255,255,255,0.55);
    line-height: 1.4;
  }

  /* ── FOOTER ── */
  footer {
    background: var(--navy);
    padding: 2rem 1.5rem;
    text-align: center;
    border-top: 1px solid rgba(255,255,255,0.06);
    color: rgba(255,255,255,0.3);
    font-size: 0.78rem;
  }

  /* ── NOTICE BANNER ── */
  .voucher-banner {
    position: fixed;
    top: 0; left: 0; right: 0;
    z-index: 999;
    background: #0d1a0d;
    border-bottom: 1px solid rgba(255,184,0,0.15);
    padding: 0 1rem 0 0;
    display: flex;
    align-items: stretch;
    min-height: 62px;
    transition: transform 0.4s ease, opacity 0.4s ease;
  }

  .voucher-label {
    background: var(--solar);
    color: var(--navy);
    font-size: 0.68rem;
    font-weight: 900;
    letter-spacing: 0.13em;
    text-transform: uppercase;
    padding: 0 1.2rem;
    display: flex;
    align-items: center;
    white-space: nowrap;
    flex-shrink: 0;
  }

  .voucher-text {
    font-size: 0.88rem;
    font-weight: 400;
    color: rgba(255,255,255,0.7);
    line-height: 1.5;
    padding: 0.85rem 1.4rem;
    flex: 1;
  }

  .voucher-text strong {
    color: #FFD166;
    font-weight: 700;
  }

  .voucher-divider {
    width: 1px;
    background: rgba(255,255,255,0.08);
    margin: 0.6rem 0;
    flex-shrink: 0;
  }

  .voucher-aside {
    display: flex;
    align-items: center;
    padding: 0 1.3rem;
    font-size: 0.75rem;
    color: rgba(255,184,0,0.45);
    font-style: italic;
    white-space: nowrap;
    flex-shrink: 0;
  }

  body.has-banner { padding-top: 62px; }

  .voucher-banner.hidden {
    transform: translateY(-100%);
    opacity: 0;
    pointer-events: none;
  }

  @media (max-width: 600px) {
    /* --- VOUCHER BANNER --- */
    .voucher-banner {
      min-height: 48px;
    }
    .voucher-label {
      font-size: 0.58rem;
      padding: 0 0.75rem;
      letter-spacing: 0.1em;
    }
    .voucher-text {
      font-size: 0.8rem;
      padding: 0.55rem 0.9rem;
    }
    .voucher-aside, .voucher-divider { display: none; }
    .v-full { display: none; }
    .v-short { display: inline; }
    body.has-banner { padding-top: 48px; }

    /* --- HERO --- */
    .hero {
      padding: 190px 1.2rem 3rem;
      justify-content: flex-start;
    }
    .logo {
      font-size: 0.75rem;
      letter-spacing: 0.18em;
      margin-bottom: 0.5rem;
    }
    .hero-tag {
      margin-bottom: 0.75rem;
    }
    .hero h1 {
      font-size: clamp(2rem, 10vw, 3rem);
      line-height: 1.1;
      margin-bottom: 0;
    }
    .hero p {
      font-size: 0.95rem;
      margin: 0.75rem auto 0;
      max-width: 90%;
    }
    .cta-btn {
      font-size: 1rem;
      padding: 0.9rem 2rem;
      width: 100%;
      max-width: 320px;
      margin-top: 1.25rem;
    }
    .trust-strip {
      gap: 0.75rem;
      flex-direction: column;
      align-items: center;
      margin-top: 1.25rem;
    }
    .trust-item {
      font-size: 0.85rem;
    }

    /* --- LOGO G --- */
    .brand-logo-wrap {
      margin-top: 1.5rem;
      margin-bottom: -45px;
    }
    .brand-logo {
      width: 85px;
      height: 85px;
    }

    /* --- STATS --- */
    .stats-grid {
      grid-template-columns: 1fr 1fr;
      gap: 0.75rem;
      padding: 1.5rem 1rem;
    }
    .stat-card {
      padding: 1rem 0.75rem;
    }
    .stat-num {
      font-size: 1.8rem;
    }
    .stat-label {
      font-size: 0.72rem;
    }

    /* --- FORM SECTION --- */
    .form-section {
      padding: 2.5rem 1.2rem 3rem;
    }
    .section-title {
      font-size: 1.5rem;
    }
    .section-sub {
      font-size: 0.88rem;
    }
    .step-card {
      padding: 1.5rem 1.25rem;
    }
    .form-input {
      font-size: 1rem;
      padding: 0.9rem 1rem;
    }
    .btn-primary {
      width: 100%;
      font-size: 1rem;
      padding: 0.9rem;
    }

    /* --- STEPPER --- */
    .stepper {
      gap: 0;
    }
    .step-label {
      font-size: 0.65rem;
    }

    /* --- SUN ORB --- */
    .sun-orb {
      width: 220px;
      height: 220px;
      top: -40px;
    }
  }
  @media (min-width: 601px) {
    .v-short { display: none; }
  }

  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(20px); }
    to   { opacity: 1; transform: translateY(0); }
  }

  @media (max-width: 480px) {
    .card { padding: 2rem 1.25rem; }
    .step-label { font-size: 0.6rem; }
    .step-circle { width: 38px; height: 38px; font-size: 0.8rem; }
  }
</style>
</head>
<body>

<!-- NOTICE BANNER -->
<div class="voucher-banner" id="voucherBanner">
  <div class="voucher-label">Aviso importante</div>
  <div class="voucher-text">
    <span class="v-full">Hay <strong>vouchers de incentivo disponibles</strong> y muy pocas personas saben que los pueden utilizar. Cupos por orden de llegada.</span>
    <span class="v-short"><strong>Vouchers disponibles</strong> — pocos lo saben. Cupos limitados.</span>
  </div>
  <div class="voucher-divider"></div>
  <div class="voucher-aside">Disponibilidad limitada</div>
</div>

<!-- HERO -->
<section class="hero">
  <div class="sun-orb"></div>
  <div class="logo">☀ PlanetSolarPR</div>

  <div class="hero-tag">
    <span class="dot-live"></span>
    Evaluaciones disponibles ahora mismo
  </div>

  <h1>Deja de pagarle tanto a <span>LUMA</span></h1>

  <a href="#empezar" class="cta-btn">
    Calcular mi ahorro gratis
    <span class="arrow-icon">→</span>
  </a>

  <p class="hero-sub">
    Descubre en menos de 3 minutos cuánto puedes ahorrar en tu factura eléctrica. Sin compromiso, sin visita, sin complicaciones.
  </p>

  <div class="trust-strip">
    <div class="trust-item"><span>✓</span> Sin costo inicial</div>
    <div class="trust-item"><span>✓</span> Respuesta en menos de 24hr</div>
    <div class="trust-item"><span>✓</span> 100% confidencial</div>
  </div>
</section>

<!-- STATS -->
<section class="stats-section">
  <div class="stats-grid">
    <div class="stat-card">
      <div class="stat-num">$0</div>
      <div class="stat-label">Costo de evaluación</div>
    </div>
    <div class="stat-card">
      <div class="stat-num">77%</div>
      <div class="stat-label">Reducción promedio de factura mensual</div>
    </div>
    <div class="stat-card">
      <div class="stat-num">+5,000</div>
      <div class="stat-label">Personas con energía renovable en Puerto Rico</div>
    </div>
    <div class="stat-card">
      <div class="stat-num">-24hr</div>
      <div class="stat-label">Tiempo de respuesta garantizado — de no cumplir descuentos serán aplicados</div>
    </div>
  </div>
</section>

<!-- FORM SECTION -->
<section class="form-section" id="empezar">

  <div class="section-label">Empieza aquí</div>
  <h2 class="section-title">Obtén tu evaluación gratis</h2>
  <p class="section-sub">Llena tus datos y te contactamos por WhatsApp con tu propuesta personalizada.</p>

  <div class="card" style="max-width:480px;margin:0 auto;">
    <label for="name">¿Cómo te llamamos?</label>
    <input class="form-input" type="text" id="name" placeholder="Tu nombre">
    <div class="error-msg" id="nameError">Por favor ingresa tu nombre.</div>

    <a id="waBtn" href="#" onclick="return openWhatsApp()" class="cta-btn" style="display:flex;align-items:center;justify-content:center;gap:0.6rem;margin-top:1.5rem;text-decoration:none;font-size:1.05rem;">
      <svg width="22" height="22" viewBox="0 0 24 24" fill="currentColor"><path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347z"/><path d="M12 0C5.373 0 0 5.373 0 12c0 2.123.554 4.116 1.523 5.845L0 24l6.335-1.497A11.954 11.954 0 0012 24c6.627 0 12-5.373 12-12S18.627 0 12 0zm0 21.818a9.8 9.8 0 01-5.002-1.368l-.36-.213-3.722.879.936-3.618-.235-.372A9.787 9.787 0 012.182 12C2.182 6.57 6.57 2.182 12 2.182S21.818 6.57 21.818 12 17.43 21.818 12 21.818z"/></svg>
      Contactar por WhatsApp
    </a>

    <div id="locStatus" style="display:flex;align-items:center;gap:0.5rem;font-size:0.78rem;color:#94A3B8;margin-top:1rem;justify-content:center;">
      <span id="locIcon">📍</span>
      <span id="locText">Obteniendo tu ubicación...</span>
    </div>
    <p style="text-align:center;font-size:0.75rem;color:#94A3B8;margin-top:0.75rem;">
      ✓ Sin costo &nbsp;·&nbsp; ✓ Respuesta en menos de 24hr &nbsp;·&nbsp; ✓ 100% confidencial
    </p>
  </div>

</section>

<footer>
  © 2025 PlanetSolarPR · Tu información está protegida y nunca se comparte con terceros.
</footer>

<script>
  // Hide voucher banner when form section becomes visible
  (function() {
    const banner = document.getElementById('voucherBanner');
    const target = document.getElementById('empezar');
    if (banner && target) {
      const observer = new IntersectionObserver(function(entries) {
        entries.forEach(function(entry) {
          if (entry.isIntersecting) {
            banner.classList.add('hidden');
            document.body.classList.remove('has-banner');
          } else {
            banner.classList.remove('hidden');
            document.body.classList.add('has-banner');
          }
        });
      }, { threshold: 0.1 });
      observer.observe(target);
    }
  })();

  function buildAndSend(name, locLine) {
    var msg = encodeURIComponent(
      '\uD83C\uDF1E Hola, soy ' + name + ' y quiero mi evaluaci\u00F3n solar gratuita.' +
      locLine +
      '\n\n\uD83D\uDCF8 Ahora te env\u00EDo fotos de mi factura el\u00E9ctrica.'
    );
    window.location.href = 'https://wa.me/17872160220?text=' + msg;
  }

  function openWhatsApp() {
    const name = document.getElementById('name').value.trim();
    const nameErr = document.getElementById('nameError');

    if (name.length < 2) {
      nameErr.classList.add('show');
      document.getElementById('name').focus();
      return false;
    }
    nameErr.classList.remove('show');

    var btn = document.getElementById('waBtn');
    btn.style.opacity = '0.7';
    btn.style.pointerEvents = 'none';

    var locIcon = document.getElementById('locIcon');
    var locText = document.getElementById('locText');
    if (locText) { locText.textContent = 'Obteniendo ubicaci\u00F3n...'; }

    if (!navigator.geolocation) {
      buildAndSend(name, '');
      return false;
    }

    navigator.geolocation.getCurrentPosition(
      function(pos) {
        var locLine = '\nUbicaci\u00F3n: https://maps.google.com/?q=' + pos.coords.latitude + ',' + pos.coords.longitude;
        if (locIcon) locIcon.textContent = '\u2705';
        if (locText) { locText.textContent = 'Ubicaci\u00F3n capturada'; locText.style.color = '#4ade80'; }
        buildAndSend(name, locLine);
      },
      function() {
        if (locIcon) locIcon.textContent = '\uD83D\uDCCD';
        if (locText) { locText.textContent = 'Ubicaci\u00F3n no disponible'; locText.style.color = '#94A3B8'; }
        buildAndSend(name, '');
      },
      { timeout: 6000, maximumAge: 300000 }
    );
    return false;
  }
</script>
</body>
</html>
